#Create map of where samples were collected and have larger dots for areas that had more specimens collected

#Expected inputs: a .csv file that contains sample longitudes and latitudes and well as other population data, give it names of columns to color the sample dots by
#expected output: a map that contains the sample location and the more samples collected from that location the larger the symbol