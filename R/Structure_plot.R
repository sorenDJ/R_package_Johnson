#Create a structure/admixture plot that is ordered by inferred genetic cluster then longitude

#Expected inputs: .Q file, text file with population data and GPS coordinates
#Expected outputs: a structure barplot that is ordered first by inferred genetic cluster then by longitude.